server:
  port: 9010

spring:
  application:
    name: service-api

  cloud:
    config:
      name: ${spring.application.name}
      label: master
      uri: http://localhost:7010/
      profile: ${spring.profiles.active:dev}